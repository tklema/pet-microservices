spring:
  datasource:
    url: jdbc:h2:mem:orderdb
#    username: sa
#    password: password
#    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8082

user:
  service:
    url: http://localhost:8081