spring:
  datasource:
    url: jdbc:h2:mem:userdb
#    username: sa
#    password: password
#    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081